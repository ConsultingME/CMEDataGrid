({
	doSort: function (component, event, helper) {
		helper.sort(component, event, helper);
	},

	doFilter: function (component, event, helper) {
		helper.filter(component, event, helper);
	},

	handleSortChanged: function (component, event, helper) {
		helper.handleSortChanged(component, event, helper);
	},

	doInit: function (component, event, helper) {
		helper.init(component, event, helper);
	}
})